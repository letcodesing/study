#18시 캐글제출 점수확인 24시 class와 함수 정리 제출

함수는 def 로 정의한다
class는 class로 정의한다

파이썬 내에서 class는 .을 찍고 네임스페이스를 지정할 수 있지만 함수는 그렇게 할 경우 오류가 난다.

일반적으로 함수는 소문자로, 클래스는 대문자로 이름을 정의한다.

클래스 내부에 정의된 함수는 메서드다. 
변수는 클래스 내부에 정의된 특징이다.

어떤 인스턴스를 해당 클래스로 정의하면 해당클래스의 특성(변수)를 똑같이 적용받고 해당클래스의 액션(메서드)를 할 수 있게 된다.

