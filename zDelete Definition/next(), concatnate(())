next는 객체의 __next__ 메서드를 호출해줍니다. next는 기본값을 지정할 수 있습니다. 기본값을 지정하면 반복이 끝나더라도 StopIteration이 발생하지 않고 기본값을 출력합니다. next는 이터레이터에서 값을 차례대로 꺼냅니다. iterator는 순서대로 자료를 가져온 이후에는 그 데이터는 폐기해 버리기 때문에, 메모리 사용에 제약이 따르는 대규모 데이터처리에 매우 효과적입니다.

concatnate 이중괄호 - 첫번째 인수로  array를 받으며 이것이 바뀌면 안되기 때문이다.(두번째 인수인 axis에 의해 정의되지 않는 이상)